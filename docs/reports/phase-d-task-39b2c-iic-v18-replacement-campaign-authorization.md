# Phase D Task 39B2c ii-c-c v18 replacement campaign authorization

> Status: authorized / pending / unconsumed
>
> Authorized source baseline: `7054cedc401075cde97658f87511db105be14483`
>
> Authorized source tree: `261534676bb88500ee8f8433a2884a166b8882d2`

## Authorization

The user explicitly authorized exactly one fresh privileged Task 39 ii-c
replacement machine campaign based on the pushed persistent-evidence checkpoint
above. This one-shot authorization is named v18. Campaigns v8 through v13 and
v16 are consumed and non-retryable. v14, v15 and v17 were superseded before
launch and cannot be reused or transferred.

The earlier authorization message that named nonexistent commit
`60c3c08fe4c89daac5b44518324af79d12870620` failed the mandatory pre-launch
commit check and caused no campaign UUID, attempt UUID, install, sudo invocation
or root action. The user then explicitly reauthorized the real pushed commit
`7054cedc401075cde97658f87511db105be14483`.

The authorized invocation may generate exactly one fresh nonzero campaign UUID,
one fresh nonzero attempt UUID and one fresh owner-private evidence parent under
the current account's physical `Library/Application Support`. It must preserve
and revalidate the exact v13 and v16 persistent Gate attempts and capsules.

This authorization-only descendant changes only this record and the reports
index. Production Swift, tests, lifecycle scripts, prompts, campaign horizons,
App/helper/driver/Gate/coordinator binaries and containment boundaries remain
byte-identical to the authorized source baseline.

## Exact authority boundary

The authorization permits only the existing fixed ii-c workflow:

- build the current-source Debug diagnostic App and package-only zero-argument
  campaign executable;
- install only the fixed Stornaut App/helper/driver/Gate/coordinator and fixed
  `com.eriklee.stornaut.lifecycle` plist through the sealed lifecycle script;
- run the non-executing `/usr/bin/sudo -knv` policy probe;
- after durable arm, launch the fixed installed driver at most once through the
  existing `/usr/bin/sudo -N -p` path;
- let the trusted operator personally enter administrator credentials only after
  seeing the exact fixed install, driver or uninstall prompt;
- run the existing eight-scenario no-model cohort, independent verifier, fixed
  uninstall and zero-runtime-residue observation while retaining the permanent
  owner lock and exact v13/v16 attempts and capsules.

The Coding Agent must not read, request in chat, record, paste, synthesize or
type administrator credentials. Credentials are handled only by the fixed
controlling-TTY paths, and the bounded single-attempt relay clears its buffer on
every path.

No Codex write authority, localhost/private/link-local/Unix access, Executor,
Trash, Policy, Registered Action, release/notarization, Task 40 start, product
Deep Dive availability, L3c3d or L3c4 is authorized here.

## One-shot and stop rules

- Before `armedConsumed`, a failure may continue only through exact uninstall
  and zero-residue proof. Installed-state uncertainty stops.
- Once v18 durably records `armedConsumed`, the attempt is consumed. Any
  failure, uncertainty, cancellation, timeout or verifier rejection stops and
  forbids automatic retry or another campaign.
- No historical artifact, receipt, evidence root or persistent Gate state may be
  modified, deleted, reconstructed, appended to or reinterpreted except for the
  fixed stale-cleanup behavior already admitted by the source contract. The v13
  and v16 Gate attempts and capsules remain immutable.
- A green independently verified v18 cohort may unblock L3c3d. It does not by
  itself complete Task 39 or enable product Deep Dive.
- L3c3d remains the sole authenticated real-model step. L3c4 remains the sole
  readiness/full-verifier admission step. Task 40 remains blocked until Task 39
  produces and pushes its Ready baseline.

## Pre-launch requirements

Before root/install/sudo action, this authorization-only descendant must be
clean, independently reviewed and pushed; its parent must be the exact authorized
source baseline; all non-document product/test/verifier paths must be identical
to that parent; the fixed runtime must be absent; the historical Caches Gate must
be absent; the Application Support Gate must contain the permanent owner lock and
exact retained v13/v16 attempts and capsules; and the current narrow source,
component and evidence gates must be green. The campaign is then launched exactly
once from an interactive controlling Terminal.

No v18 campaign UUID, attempt UUID or evidence parent exists at authorization
record time. Those identities are generated and durably bound only by the unique
executable launch.
