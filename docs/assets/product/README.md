# Product Screenshots

These images are captured from the actual locally built Stornaut macOS app.
They use deterministic built-in demo data and contain no host filesystem data.

- `review-reclaim-plan-dark.webp`: Debug App, Dark appearance, Review Reclaim
  Plan using the built-in `default` Review fixture. Captured at Retina scale,
  cropped to the primary decision surface, resized to 1800 px wide, and encoded
  as WebP quality 90.
